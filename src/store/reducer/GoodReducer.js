

//初始化数据 可以被共享
const initState = {
    arr: []
}

export default function goodRecdcer (state = initState , action ){
    switch (action.type) {
        case 'add':

        return state   
        default:
        return state   

    }
}